import DisciplinePlaceholder from "../components/DisciplinePlaceholder";

export default function Animation() {
  return (
    <DisciplinePlaceholder
      number="(04)"
      title="Animation"
      intro="Character design, modeling, rigging, skinning and animation — like the R10T biped — plus 3D sculpting in Nomad Sculp."
    />
  );
}
